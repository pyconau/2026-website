---
const df = new Intl.DateTimeFormat("en-AU", { dateStyle: "long", timeZone: "Australia/Brisbane" });

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<>
  {df.format(date)}
</>
