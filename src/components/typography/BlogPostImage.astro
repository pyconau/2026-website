---
interface Props {
  src: string;
  alt: string;
  title: string;
  class?: string;
}

const { src, alt, title, class: className } = Astro.props;
---

<img src={src} class:list={["max-w-[90%] mx-auto", className]} alt={alt} title={title} />
