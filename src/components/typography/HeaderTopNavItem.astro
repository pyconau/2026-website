---
import { twMerge } from "tailwind-merge";

interface Props {
  href: string;
  className?: string;
}

const { href, className } = Astro.props;
---

<a
  href={href}
  class={twMerge("font-sans font-semibold hover:text-emerald", className)}
  ><slot /></a
>
